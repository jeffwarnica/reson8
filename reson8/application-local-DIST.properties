# -----------------------------------------------------------------------------
# Template only — copy to application-local.properties (gitignored) if you want
# file-based overrides. Never commit credentials or cluster-specific Routes here.
#
# Prefer env indirection for outside-cluster dev:
#   export RESON8_K8S_THANOS_BASE_URL=https://thanos-querier-openshift-monitoring.apps.<your-cluster-domain>/
#
# Optional file workflow from this directory:
#   cp application-local-DIST.properties application-local.properties
#   # edit placeholders; then:
#   QUARKUS_CONFIG_LOCATIONS=optional:file:./application-local.properties ./mvnw quarkus:dev
# -----------------------------------------------------------------------------
#
# %dev.reson8.k8s.thanos.base-url=https://thanos-querier-openshift-monitoring.apps.<your-cluster-domain>/
# %dev.reson8.k8s.thanos.ignore-certs=true
